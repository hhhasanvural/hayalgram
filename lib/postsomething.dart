import 'package:flutter/material.dart';

class UserPostsomething extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('POSTSOMETHING'),),

    );
  }
}
